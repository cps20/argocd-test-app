apiVersion: v1
kind: Service
metadata:
  name: argocd-test-service
  labels:
    app: argocd-test
spec:
  type: NodePort
  selector:
    app: argocd-test
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
